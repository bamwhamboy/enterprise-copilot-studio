"""Planner interface.

Sprint 4 defines the contract only: ``planner.py`` (the ``Planner`` ABC
with ``plan()``/``execute()``) and ``task.py`` (the ``Task`` model).

No concrete planner and no LangGraph — this package exists so the
module boundary and interface are fixed before a real orchestrator is
built.
"""
