"""File-level metadata extraction (size, MIME type, filenames, timestamps).

Complements the parser (which extracts content-level metadata: page
count, text) with the generic, file-system-level metadata that applies
to any upload.
"""
