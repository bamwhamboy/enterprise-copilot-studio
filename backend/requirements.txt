# Enterprise Copilot Studio — Backend Foundation
# Only dependencies actually imported by this phase's code are listed here.
# Redis, Qdrant, LiteLLM, LangGraph, and LlamaIndex clients will be added
# in the phase that implements those integrations.

# --- Web framework -----------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Validation & settings ----------------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Database -------------------------------------------------------------
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0

# --- Testing ---------------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1

# --- Sprint 3A: Knowledge ingestion --------------------------------------
python-multipart==0.0.20
pymupdf==1.25.2
aiofiles==24.1.0
